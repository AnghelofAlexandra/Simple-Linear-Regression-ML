# Simple-Linear-Regression-ML
This project implements the Simple Linear Regression algorithm in C++, utilizing the Gradient Descent method to find the optimal parameters (slope and intercept). The code includes functionalities for reading data from a CSV file and performing Min-Max scaling (normalization) for faster and more stable algorithm convergence.
